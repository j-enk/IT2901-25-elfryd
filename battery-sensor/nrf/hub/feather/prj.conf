# MQTT configuration
CONFIG_MQTT_LIB=y
CONFIG_MQTT_LIB_TLS=y

# Use MQTT configuration from Kconfig
CONFIG_MQTT_BUFFER_SIZE=2048
CONFIG_MQTT_CONNECT_ATTEMPTS=3
CONFIG_MQTT_CONNECT_RETRY_DELAY_MS=5000
CONFIG_MQTT_CONNECT_TIMEOUT_MS=5000
CONFIG_MQTT_SLEEP_MS=5000
CONFIG_MQTT_CLIENT_ID="elfryd_hub"
CONFIG_MQTT_BROKER_HOSTNAME="elfryd.northeurope.cloudapp.azure.com"
CONFIG_MQTT_BROKER_PORT=8885
CONFIG_MQTT_TOPIC="hub/data"
CONFIG_MQTT_TLS_SEC_TAG=42

# DNS configuration
CONFIG_DNS_RESOLVER=y
CONFIG_DNS_SERVER_IP_ADDRESSES=y
CONFIG_DNS_SERVER1="8.8.8.8"
CONFIG_DNS_SERVER2="1.1.1.1"

# Networking configuration
CONFIG_NETWORKING=y
CONFIG_NET_SOCKETS=y
CONFIG_NET_SOCKETS_OFFLOAD=y

# Modem library and LTE connectivity
CONFIG_NRF_MODEM_LIB=y
CONFIG_LTE_LINK_CONTROL=y
CONFIG_LTE_AUTO_INIT_AND_CONNECT=n
CONFIG_LTE_NETWORK_MODE_LTE_M_GPS=y
# CONFIG_LTE_EDRX_ENABLE=y
# CONFIG_LTE_PSM_ENABLE=y

# Security
CONFIG_MODEM_KEY_MGMT=y

# General system settings
CONFIG_HEAP_MEM_POOL_SIZE=8192
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096

# Logging
CONFIG_LOG=y
# CONFIG_MQTT_LIB_LOG_LEVEL_DBG=y
CONFIG_LTE_LINK_CONTROL_LOG_LEVEL_INF=y

# Fix for partition issues
CONFIG_NRF_MODEM_LIB_SHMEM_TRACE_SIZE=0
# CONFIG_PM_PARTITION_SIZE_MCUBOOT=0x8000

# TFM Build fixes
CONFIG_BUILD_WITH_TFM=y
# Remove this line: CONFIG_TFM_FLASH_MERGED_BINARY=y
CONFIG_TFM_PROFILE_TYPE_MINIMAL=y

# Explicitly disable trace to avoid 0-sized partition
CONFIG_NRF_MODEM_LIB_TRACE=n

CONFIG_BOOTLOADER_MCUBOOT=y

# I2C support for sensor data collection
CONFIG_I2C=y
CONFIG_I2C_NRFX=y

# Sensor data configuration
CONFIG_ELFRYD_USE_I2C_SENSORS=n # Default to sample data, set to y to use real I2C sensors
CONFIG_ELFRYD_USE_LOCAL_TIMESTAMP=y # Default to generating timestamps locally
